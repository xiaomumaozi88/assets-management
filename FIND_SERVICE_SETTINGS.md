# 📍 如何找到服务设置（Root Directory）

## 你现在的位置

你在**项目（Project）的设置页面**，需要进入**服务（Service）的设置页面**。

---

## ✅ 正确步骤

### 1. 返回架构视图

1. 点击顶部的 **"Architecture"** 标签（不是 Settings）
2. 或点击页面左侧的 **"Architecture"** 菜单

### 2. 进入服务设置

1. 在架构视图中，你会看到 **assets-management** 服务卡片
2. **点击 assets-management 服务卡片**
3. 这会打开服务的详情页面

### 3. 找到服务设置

在服务详情页面：
- 点击顶部的 **"Settings"** 标签
- 现在你会看到**服务的设置**（不是项目的设置）

在服务设置页面，你会看到：
- **Name** - 服务名称
- **Root Directory** ⬅️ 在这里！
- **Start Command** ⬅️ 在这里！
- **Build Command** ⬅️ 在这里！

---

## 📋 导航路径

```
Railway Dashboard
  └── pacific-solace (项目)
      └── Architecture (点击这里)
          └── assets-management (点击服务卡片)
              └── Settings (点击这里)
                  └── Root Directory ⬅️ 找到了！
```

---

## 🔧 如果还是找不到

### 方法 1：直接修改 Start Command

如果找不到 Root Directory，可以在 **Start Command** 中使用完整路径：

1. 进入服务的 Settings
2. 找到 **Start Command**
3. 设置为：`cd backend && npm run start:prod`
4. 找到 **Build Command**
5. 设置为：`cd backend && npm install && npm run build`

### 方法 2：使用配置文件

我已经创建了配置文件，Railway 应该会自动识别：
- `railway.json` - Railway 配置
- `nixpacks.toml` - 构建配置

确保这些文件在根目录，然后重新部署。

---

## 💡 快速检查

在服务的 Settings 页面，你应该看到以下选项：
- ✅ Name
- ✅ Root Directory
- ✅ Start Command
- ✅ Build Command
- ✅ Restart Policy
- 等等...

如果看到的是：
- Project Info
- Visibility
- Generate Template

那说明你在**项目的设置**，需要回到 **Architecture** 页面进入**服务的设置**。

